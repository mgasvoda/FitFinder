{
    "scripts": {
      "start:api": "python -m uvicorn backend.main:app --host 0.0.0.0 --port $PORT",
      "start:chat": "python -m chainlit run backend/chainlit_app.py --host 0.0.0.0 --port $PORT"
    }
  }